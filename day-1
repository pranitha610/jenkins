pipeline {
    agent any

    stages {
        stage('clone') {
            steps {
                git branch: 'main', url: 'https://github.com/pranitha610/jenkins/blob/main'
            }
        }
        stage('init') {
            steps {
                sh 'terraform init'
            }
        }
    }
}
